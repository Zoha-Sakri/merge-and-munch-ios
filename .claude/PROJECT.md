# iOS Project Context

`Sources/MergeAndMunch` contains the SwiftUI game client. `GameStore` owns farm plots, board merges, orders, rewards, shop actions, and persistence. Keep the UI original and consistent with the farming-city loop.

Use current SwiftUI APIs and test on iOS 16+ with the latest Xcode. Do not recreate another game's protected content or identity.