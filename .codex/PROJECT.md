# iOS Codex Context

Build the original Merge & Munch SwiftUI client around farm production, ingredient merging, trade orders, town improvements, and Ancient City expeditions. Keep state changes in `GameStore` and preserve `UserDefaults` save behavior.

The source requires an Xcode iOS app target. Report simulator limitations honestly; do not claim a build without running it.